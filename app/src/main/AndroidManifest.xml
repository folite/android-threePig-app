<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <!--
    To auto-complete the email text field in the login form with the user's emails
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@drawable/hp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- screenOrientation 固定縱向 -->
        <activity
            android:name=".login_main"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register_page" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".mintPage" />
        <activity android:name=".forgetPasswd" />
        <activity android:name=".menuList" />
        <activity android:name=".mem_detail" />
        <activity android:name=".resetdatePage" />
        <!-- activity android:name=".menuListAdapter"></activity -->
        <activity android:name=".menuOrder" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Map" />
        <activity android:name=".SetPage" />
        <activity android:name=".echoPage" />
        <activity android:name=".PostList" />
        <activity android:name=".postOrder" />
        <activity android:name=".resetPassword" />
        <activity android:name=".CollectMenuPage" />
        <activity android:name=".menuTypePage" />
        <activity android:name=".couponList" />
        <activity android:name=".couponOrder" />
        <activity android:name=".shoppingCart" />
        <activity android:name=".Game">

            <!--
 ATTENTION: This intent was auto-generated. Follow instructions at
  https://g.co/AppIndexing/AndroidStudio to publish your URLs.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                  ATTENTION: This data URL was auto-generated. We recommend that you use the HTTP scheme.
                  TODO: Change the host or pathPrefix as necessary.
                -->
                <data
                    android:host="[ENTER-YOUR-HTTP-HOST-HERE]"
                    android:pathPrefix="/game"
                    android:scheme="http" />
            </intent-filter>
        </activity>
        <activity android:name=".test" />
        <activity android:name=".sale1List" />
        <activity android:name=".sale1Order" />
        <activity android:name=".sale1OrderAdd" />
        <activity android:name=".shoppingCartSend" />
        <activity android:name=".searchMenuPage" />
        <activity android:name=".finishOrderList" />
        <activity
            android:name=".selectTime"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".mainFragment" />
        <activity android:name=".Qrcode" />
        <activity android:name=".rank" />
        <activity android:name=".recommendation"></activity>
    </application>

</manifest>